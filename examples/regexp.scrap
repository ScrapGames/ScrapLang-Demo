fn main() {
  const text = "En un lugar de La Mancha, de cuyo nombre no quiero acordarme..."

  console.log(new RegExp("*lugar*").test(text)) // true
  console.log(new RegExp("lugar").test(text)) // false
}