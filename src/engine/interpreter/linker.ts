/**
 * The linker is the program piece who resolves the reference to variables.
 * With 'reference to variables', we doesn't refers to a reference value like: &name.
 * But to identifiers, who contains the values which was designed to throught
 */