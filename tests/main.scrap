from Galua import Vis, Vector, SceneObject
from std::net import http::fetch
from std import io::InputStream, io::OutputStream

fn test(arg0: string, ) {}

class Player extends SceneObject {
  var health: u8 = 10
  const position: Vector

  pub fn onCollisionEnter(collision: Vis::Collision) {}
}

fn main() {
  fetch("https://test.n8n.com/api/init")
}