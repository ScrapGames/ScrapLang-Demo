import std::{
  sys::{ sleep, process },
  net::{ http, socket }
}

import ::{
  Galua as Engine,
  Engine::{
    StateMachine,
    SceneObjectBehaviour,
    Animations::{ Clip, Animation },
    Scenes::{ Manager as SceneManager, Scene }
  },

  Engine::{ Vis::Collision, Vector3 }
}

static name = "Raxabi"

fn modify(name: String) {}

fn main() {

}